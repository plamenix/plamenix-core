/// <reference types="react" />
/**
 * Plamenix plugin UI entry point.
 *
 * Exports a default factory that the host invokes once per shell
 * mount. Return contributions (sidebar panels, inspector tabs,
 * commands, …) by calling the corresponding `register*` helpers from
 * `@plamenix/ui`.
 */

export default function activate() {
  // TODO(author): replace with real contributions. Example:
  //
  //   import { registerSidebarPanel } from '@plamenix/ui';
  //   registerSidebarPanel({ id: 'my-panel', label: 'My Panel',
  //     Component: MyPanel });
}
