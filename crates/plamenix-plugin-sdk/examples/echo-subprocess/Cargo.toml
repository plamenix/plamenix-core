[package]
name = "echo-subprocess"
version = "0.0.0"
edition = "2024"
rust-version = "1.95"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
plamenix-plugin-sdk = { path = "../.." }

[[bin]]
name = "echo-subprocess"
path = "src/main.rs"

# Standalone — opts out of the workspace so the example builds as a
# regular native binary without inheriting workspace-level overrides.
[workspace]
